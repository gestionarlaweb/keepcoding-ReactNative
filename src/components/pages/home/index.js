import component from './container';
export default component;
// Este index.js me va ha exportar el ./container.js en vez de ./view
// Para que nuestra View pase primero por el container.js y lo conectems a REDUX y luego ya se exporte
